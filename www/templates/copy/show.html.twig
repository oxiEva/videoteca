{% extends 'base.html.twig' %}

{% block title %}Copy{% endblock %}

{% block body %}
    <h1>Copy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ copy.id }}</td>
            </tr>
            <tr>
                <th>Film Title</th>
                <td>{{ copy.title }}</td>
            </tr>
            <tr>
                <th>FilmId</th>
                <td>{{ copy.film.title }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ copy.price }}</td>
            </tr>
            <tr>
                <th>Language</th>
                <td>{{ copy.language }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ copy.status }}</td>
            </tr>
            <tr>
                <th>DateOfSale</th>
                <td>{{ copy.dateOfSale ? copy.dateOfSale|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>CreationDate</th>
                <td>{{ copy.creationDate ? copy.creationDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('copy_index') }}">back to list</a>

    <a href="{{ path('copy_edit', {'id': copy.id}) }}">edit</a>

    {{ include('copy/_delete_form.html.twig') }}
{% endblock %}
